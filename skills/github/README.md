# GitHub

Interact with GitHub using the `gh` CLI. Supports `gh issue`, `gh pr`, `gh run`, and `gh api` for issues, PRs, CI runs, and advanced queries directly from the agent.

## Installation

```bash
pi install path/to/pcaropi/skills/github
```