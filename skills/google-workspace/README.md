# Google Workspace (No MCP)

Access Google Workspace APIs (Drive, Docs, Calendar, Gmail, Sheets, Slides, Chat, People) via local helper scripts without the need for an MCP server. Handles OAuth login and direct API calls.

## Installation

```bash
pi install path/to/pcaropi/skills/google-workspace
```