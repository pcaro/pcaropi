# Summarize

Fetch a URL or convert binary files (PDF/DOCX/PPTX) to Markdown using `markitdown`. Useful for quickly reading web pages, analyzing binary docs, or generating short summaries before performing deeper work.

## Installation

```bash
pi install path/to/pcaropi/skills/summarize
```