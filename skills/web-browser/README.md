# Web Browser

Interact with web pages (click, fill forms, navigate) via headless Chrome CDP. Useful for collaborative site exploration, testing UIs, or web automation.

## Installation

```bash
pi install path/to/pcaropi/skills/web-browser
```